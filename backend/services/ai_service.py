def generate_travel_plan(user_prompt: str) -> str:
    # TODO ATPP-27: Replace with real Ollama call once Docker container is confirmed
    return f"Mock travel plan generated for prompt: {user_prompt}"
